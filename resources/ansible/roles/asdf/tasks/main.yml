---
- name: checkout asdf
  git:
    repo: https://github.com/asdf-vm/asdf.git
    dest: "{{ asdf_dir }}"
    version: "{{ asdf_version }}"

- name: add asdf to bashrc
  template: src=asdf.sh dest="{{ bash_source_dir_path }}/000-asdf.sh"
